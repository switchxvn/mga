{
  "name": "frontend-old",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/frontend-old/src",
  "// targets": "to see all targets run: nx show project ew --web",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/frontend-old && npx nuxt dev --dotenv apps/frontend-old/.env --no-typecheck"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/frontend-old && npx nuxt build"
      }
    }
  }
}
