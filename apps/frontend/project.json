{
  "name": "frontend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/frontend/src",
  "// targets": "to see all targets run: nx show project ew --web",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/frontend && env -u PORT npx nuxt dev --dotenv ../../.env --no-typecheck"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/frontend && npx nuxt build"
      }
    }
  }
}
